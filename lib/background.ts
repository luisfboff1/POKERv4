// Função utilitária para fundo padrão dos componentes UI
export function getDefaultBackground() {
  // Verde mais claro para fundo padrão
  return {
    backgroundColor: '#3a6b4b', // verde mais claro
    darkBackgroundColor: '#25422f', // verde escuro para dark mode
  };
}

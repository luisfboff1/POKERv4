# 🔧 Arquivo de Exemplo - Variáveis de Ambiente
# Copie este arquivo para .env.local e preencha com seus valores

# ==============================================
# 📄 INSTRUÇÕES DE USO
# ==============================================
# 1. Copie: cp .env.local.example .env.local
# 2. Preencha os valores abaixo
# 3. Execute: npm run build
# 4. O sistema vai gerar automaticamente:
#    - dist/api/.env (para PHP)
#    - .env.local (para Next.js)

# ==============================================
# 🌐 VARIÁVEIS PÚBLICAS (Next.js - NEXT_PUBLIC_*)
# ==============================================
# Essas variáveis são expostas no frontend

# URL base da API (geralmente /api em produção)
NEXT_PUBLIC_API_URL=/api

# Chave da API do GROQ (para IA/ChatBot)
NEXT_PUBLIC_GROQ_API_KEY=your_groq_api_key_here

# ==============================================
# 🔒 VARIÁVEIS PRIVADAS (PHP Backend)
# ==============================================
# Essas variáveis são usadas apenas no servidor

# Configurações do Banco de Dados
DB_HOST=localhost
DB_NAME=poker_database
DB_USER=your_db_user
DB_PASSWORD=your_db_password

# JWT para autenticação
JWT_SECRET=your_super_secret_jwt_key_here

# Configurações SMTP (envio de emails)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your_email@gmail.com
SMTP_PASSWORD=your_app_password
SMTP_ENCRYPTION=tls

# Chave API GROQ (backend)
GROQ_API_KEY=your_groq_api_key_here

# ==============================================
# 📝 NOTAS IMPORTANTES
# ==============================================
# 
# - Este arquivo (.env.local.example) deve ser versionado
# - O arquivo .env.local NÃO deve ser versionado (já está no .gitignore)
# - Em produção (GitHub Actions), essas variáveis vêm dos Secrets
# - O script build.js automaticamente organiza as variáveis:
#   * NEXT_PUBLIC_* → .env.local (Next.js)
#   * Demais → dist/api/.env (PHP)
#
# ==============================================
# 🚀 AMBIENTES
# ==============================================
#
# LOCAL (desenvolvimento):
# - Carrega deste arquivo → .env.local
# - npm run build usa essas variáveis
#
# PRODUÇÃO (GitHub Actions):
# - Carrega dos GitHub Secrets
# - Mesmo script build.js funciona automaticamente
#
# RESULTADO:
# - dist/ sempre idêntico, independente do ambiente
#